name: Hello Github Actions        # Name of the workflow

on: [push]                        # Trigger: Run the workflow on every git push

jobs:
  example:                        # Job name
    runs-on: ubuntu-latest        # Runs the job on an Ubuntu runner
    steps:                        # List of steps to run
      - name: Test
        run: |
          echo "Hello World"

      - name: Test 2
        run: |
          echo "Goodbye World"